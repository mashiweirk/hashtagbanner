/* These are JS onload */

window.onload = function () {
		//alert(globTxt.intro);
};

//window.onload = init();
//window.addEventListener('resize', resizeProfile, false);
//window.addEventListener('orientationchange', resizeProfile, false);